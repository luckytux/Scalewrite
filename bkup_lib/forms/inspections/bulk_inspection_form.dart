// File: lib/forms/inspections/bulk_inspection_form.dart

import 'package:flutter/material.dart';

class BulkInspectionForm extends StatelessWidget {
  const BulkInspectionForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bulk Inspection Form')),
      body: const Center(
        child: Text('Bulk Inspection form coming soon.'),
      ),
    );
  }
}
